// monteCarlo - A higher order Monte Carlo simulator function.
var monteCarlo = function (numTrials, usersFunction, input){
    var outcomeArr = []
    for (i = 0; i < numTrials; i++) {
        var value = usersFunction(input);   
    }
};